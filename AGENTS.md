# AGENTS

- Do not add, modify, or run unit tests. Do not use TDD. Agent evaluation cases are not unit tests and may be created and executed when they are part of the task.
- Do not call a planning agent. Plan and track the tasks yourself
- Do not run simplify unless the user explicitly requests it
