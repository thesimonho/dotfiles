# Superseded cluster layout

Superseded by the current GitOps runbook and repository codemap.

The retired lab hosted Git at `gitea.lab.internal` on `10.0.10.8`. Operators sometimes restarted deployments after source timeouts. This document is retained only for historical incident review.
