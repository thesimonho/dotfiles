layout {
    swap_tiled_layout name="auto_grid" {
        // 1 pane
        tab max_panes=1 {
            pane
        }
        // 2 panes: vertical stack
        tab max_panes=2 {
            pane split_direction="vertical" {
                pane
                pane
            }
        }
        // 3 panes: left full-height, right stacked
        tab max_panes=3 {
            pane split_direction="horizontal" {
                pane
                pane split_direction="vertical" {
                    pane
                    pane
                }
            }
        }
        // 4 panes: 2x2 grid
        tab max_panes=4 {
            pane split_direction="horizontal" {
                pane split_direction="vertical" {
                    pane
                    pane
                }
                pane split_direction="vertical" {
                    pane
                    pane
                }
            }
        }
    }
    default_tab_template {
        children
        pane size=1 borderless=true {
            plugin location="file:~/.config/zellij/plugins/zjstatus.wasm" {
                format_left "{mode} {swap_layout} #[fg=green,bold]{session} {tabs}"
                format_center ""
                format_right "{pipe_zjstatus_hints} {datetime}"
                format_space ""
                pipe_zjstatus_hints_format "{output}"
                border_enabled "true"
                border_char "─"
                border_format "#[fg=white]{char}"
                border_position "top"
                hide_frame_for_single_pane "false"
                mode_normal "#[fg=blue]"
                tab_normal "#[fg=white] {index} {name} {fullscreen_indicator}{sync_indicator}{floating_indicator}"
                tab_active "#[fg=white,bold,bg=black] {index} {name} {fullscreen_indicator}{sync_indicator}{floating_indicator}"
                tab_separator "#[fg=white, bg=black] | "
                tab_fullscreen_indicator "□ "
                tab_sync_indicator "  "
                tab_floating_indicator "󰉈 "
                datetime "#[fg=black,bold] {format} "
                datetime_format "%a %d %b"
                datetime_timezone "America/Vancouver"
                swap_layout_format "#[fg=magenta] {name} "
                swap_layout_hide_if_empty "true"
            }
        }
    }
}

